# USE PYTHON 3.13.X, PREFERABLY 3.13.9
--index-url https://download.pytorch.org/whl/cu129
--extra-index-url https://pypi.org/simple

accelerate==1.11.0  # * This requirement is not relevant now, but might be later for optimizations
chromadb==1.3.4
hf_xet==1.2.0
huggingface-hub==0.35.3
langchain-chroma==1.0.0
langchain-community==0.4.1
langchain-huggingface==1.0.1
pypdf==6.1.3
sentence-transformers==5.1.2
torch==2.9.0
torchvision==0.24.0
torchaudio==2.9.0
yaspin==3.3.0

# Install CUDA toolkit for your distribution first
# Example for Arch Linux:
#   sudo pacman -S cuda cuda-tools cudnn
llama-cpp-python -C cmake.args="-DGGML_CUDA=on"

# If you're running into issues or can't build it from source,
# you can use pre-built wheels with CUDA support.
# llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu122 # Uncomment this and comment the other line
